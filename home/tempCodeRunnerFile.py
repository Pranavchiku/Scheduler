start_time.append(datetime.timestamp(now1))
    # duration.append(datetime.timestamp(resp2[i]["duration"]))

    # then=resp2[i]["start_time"]
    # timeLeft.append(then-(datetime.now()))
    # event.append(resp2[i]["name"])
